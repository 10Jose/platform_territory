# En desarrollo, déjalo comentado: el front usa package.json "proxy" hacia el BFF (puerto 8000).
# Descomenta solo si necesitas forzar la URL (p. ej. otro host).
# REACT_APP_API_URL=http://localhost:8000